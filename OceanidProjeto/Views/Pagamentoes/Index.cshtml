@model IEnumerable<OceanidProjeto.Models.Pagamento>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.status)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.metodo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.status)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.metodo)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.idPag">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.idPag">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.idPag">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
